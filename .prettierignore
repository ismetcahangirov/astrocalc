node_modules
dist
build
coverage
.expo
.next
.automation
package-lock.json

# Expo CNG-generated native projects (regenerated by `expo prebuild`)
apps/*/android
apps/*/ios

# drizzle-kit-generated migration snapshots (the tool owns their format)
apps/*/drizzle

# Hand-authored prose docs — not auto-formatted (Prettier's markdown table/
# list reflow fights the existing hand-wrapped style). Code style tooling
# governs apps/*, packages/calc-engine, and repo config, not doc prose.
*.md
